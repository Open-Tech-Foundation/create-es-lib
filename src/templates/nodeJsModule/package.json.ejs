{
    "name": "<%= pkgNameWithScope %>",
    "version": "0.1.0",
    "description": "",
    "keywords": [],
    "author": {
        name: "<%= authorFullName %>",
        email: "<%= authorEmail %>",
        url: ""
    },
    "license": "<%= lic %>",
    "homepage": "",
    "type": "module",
    "module": "lib/<%= pkgName %>.js",
    "scripts": {
        <% if (bundler && bundler === 'rollup') { %>
            "build": "rollup -c",
            "build:w": "rollup -c -w",         
        <% } %>
        <% if (testRunner === 'jest') { %>
            "test": "NODE_OPTIONS=--experimental-vm-modules jest"
        <% } else { %>
             "test": "echo \"Error: no test specified\" && exit 1"
        <% } %>
    },
    "files": [
    <% if (buildDir) { %>
     "<%= buildDir %>"
    <% } %>
    ]
}