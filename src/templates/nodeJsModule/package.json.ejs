{
    "name": "<%= pkgName %>",
    "version": "0.1.0",
    "description": "",
    "keywords": [],
    "author": {
        name: "<%= authorFullName %>",
        email: "<%= authorEmail %>",
        url: ""
    },
    "license": "<%= lic %>",
    "homepage": "",
    "type": "module",
    "module": "lib/<%= libName %>.esm.js",
    "scripts": {
        <% if (bundler && bundler === 'rollup') { %>
            "build": "rollup -c",
            "build:w": "rollup -c -w",         
        <% } %>
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "files": [
    <% if (buildDir) { %>
     "<%= buildDir %> "
    <% } %>
    ]
}