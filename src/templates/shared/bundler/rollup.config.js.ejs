<% if (ts) { -%>
import typescript from '@rollup/plugin-typescript'; 
<% } -%>

export default {
  input: '<%= ts ? 'src/index.ts' : 'src/index.js' %>',
  output: {
    file: <%- `'${buildDir}/${pkgName}.js'` %>,
    format: 'esm',
  },
  plugins: [
    <% if (ts) { %>
      typescript()
    <% } %>
  ],
  external: [],
};
